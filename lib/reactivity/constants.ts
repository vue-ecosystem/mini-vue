export enum ReactiveFlags {
  IS_REF = '__v_isRef',
  IS_READONLY = '__v_isReadonly',
  SKIP = '__v_skip',
  RAW = '__v_raw',
  
}

export enum TargetType {
  INVALID = 0,
  COMMON = 1,
  COLLECTION = 2,
}
